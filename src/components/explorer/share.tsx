import {Button} from "@radix-ui/themes";
import React from "react";

export default function Share() {
    return (
        <>
            <Button
                color="blue" style={{width: 75}}
                onClick={() => {
                // setStep(1)
            }}>Share</Button>
        </>
    )
}